# PostgreSQL connection for simlytics.
# Copy this file to .env and fill in your values (.env is gitignored).
PGHOST=localhost
PGPORT=5432
PGDATABASE=your_database
PGUSER=your_user
PGPASSWORD=your_password